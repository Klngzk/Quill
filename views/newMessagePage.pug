extends layouts/main-layout.pug

block content
    .chatPageContainer 
        .chatTitleBar 
            label(for="userSearchBox") To:
            #selectedUsers 
                input#userSearchBox(type="text",placeholder="Type the name of the person")
        .resultsContainer
        button#createChatButton(disabled) Create chat
